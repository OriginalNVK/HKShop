@model IEnumerable<HKShop.DTOs.CategoryMenuDto>
@{
}

<div id="responsive-nav">
	<!-- NAV -->
	<ul class="main-nav nav navbar-nav">
		<li><a href="/">Home Page</a></li>
		<li><a href="/HangHoa">Visit Store</a></li>
		@foreach (var item in Model)
		{
			<li>
                <a asp-controller="Product" asp-action="Index" asp-route-MaLoai="@item.CategoryId">
                    <i class="fas fa-apple-alt me-2"></i> @item.CategoryName
				</a>
			</li>
		}
	</ul>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        // Lấy URL hiện tại
        const currentUrl = window.location.pathname + window.location.search;

        // Lấy tất cả các thẻ <a> trong menu
        const navLinks = document.querySelectorAll(".main-nav a");

        navLinks.forEach(link => {
            // Lấy href của thẻ <a>
            const linkUrl = link.getAttribute("href");

            // So sánh URL hiện tại với href
            if (currentUrl === linkUrl || (currentUrl === "/" && linkUrl === "/")) {
                // Thêm class "active" vào thẻ <li> cha
                link.parentElement.classList.add("active");
            }
        });
    });
</script>
